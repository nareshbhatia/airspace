import type { DroneService } from './DroneService';

/**
 * Stub implementation of DroneService for Step 3.
 * Step 4 will add the real RxJS playback pipeline and store bridge.
 */
export class DroneServiceImpl implements DroneService {
  onInit(): void {
    // No-op until Step 4
  }

  onDestroy(): void {
    // No-op until Step 4 (no subscriptions yet)
  }

  play(): void {
    // No-op until Step 4
  }

  pause(): void {
    // No-op until Step 4
  }

  reset(): void {
    // No-op until Step 4
  }
}
