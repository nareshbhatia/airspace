import { cn } from '../../../utils/cn';

/**
 * Mapbox + Three.js page placeholder.
 */
export function MapboxPlusThreeJsPage() {
  return (
    <div
      className={cn(
        'relative flex min-h-0 flex-1 flex-col bg-background text-foreground',
      )}
    >
      <div className="min-h-0 flex-1 w-full" />
    </div>
  );
}
